describe('Queue', () => {
  beforeEach(() => {
    // set up database
  })
  afterEach(() => {
    // tear down database
  })
  describe('constructor', () => {
    it.todo('correctly builds config')
    it.todo('correctly assigns default config values')
    it.todo('opens a table for the queue')
    it.todo('opens a dedup table for the dedup index')
    it.todo('does not open a dedup table if dedup = false')
  })
  describe('send', () => {
    //
  })
  describe('receive', () => {
    //
  })
  describe('ack', () => {
    //
  })
})
